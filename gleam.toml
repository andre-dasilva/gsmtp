name = "gsmtp"
version = "0.1.0"

description = "SMTP Client for Gleam"
licences = ["MIT"]
repository = { type = "github", user = "andre-dasilva", repo = "gsmtp" }

[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
mug = ">= 1.2.0 and < 2.0.0"
logging = ">= 1.3.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
